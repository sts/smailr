---
exim_path:       /etc/exim4
dovecot_path:    /etc/dovecot
mail_spool_path: /srv/mail

database:
    adapter:  sqlite
    database: /etc/exim4/smailr.sqlite
    timeout: 5000

password_policy:
    length: 5

